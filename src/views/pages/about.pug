extends ../base.pug

include ../partials/mixins

block container 
    main.container(data-smoothscroll="container" data-template="about")
        section.content(data-smoothscroll="content")

            